import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Amazing Web Store",
  description:
    "Discover a wide selection of premium products at Amazing Web Store. Shop for electronics, fashion, home goods, and more, all at unbeatable prices. Start browsing now!",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
